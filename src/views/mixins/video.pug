mixin video(video) 

    article.videoItem
            div.video-container
                a(href=`/videos/${video.id}`)
                    video(src=video.fileUrl) 
                div.videoText
                    h1=video.title
                    p= video.description
                    small=video.createdAt
            