extends ../base.pug

block content 

    section.join
        if error
            span.error=error
        form(method="post")
        
            input( name="oldPassword", type="password", placeholder="Old Password", required)
            input( name="newPassword", type="password", placeholder="New Password", required)
            input( name="newPassword2", type="password", placeholder="New Password Confirmaition", required)

            input(type="submit", value="Change Password!").submit